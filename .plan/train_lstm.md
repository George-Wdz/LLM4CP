# TODO: LSTM Training Script

- [x] Review `train_rnn.py` and `models/model.py` for reusable components.
- [x] Draft CLI arguments specific to LSTM baseline (hidden size, layers, dropout, data-parallel).
- [x] Implement `train_lstm.py` reusing shared utilities and ensure logging/checkpointing match other baselines.
- [ ] Validate forward pass and training loop with a dry-run command (single epoch, small batch) if feasible.
- [ ] Document launch examples for single and multi-GPU setups.
